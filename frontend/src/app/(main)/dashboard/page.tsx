import { Metadata } from "next";

export const metadata: Metadata = {
    title: 'Dashboard',
    description: 'Zaloguj się do swojego panelu pracownika StayEasy, aby sprawdzić dzisiejsze rezerwacje i dostępność pokoi.',
};

export default function Page() {
    return (
        <>
            
        </>
    );
}